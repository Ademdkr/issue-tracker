# Database Configuration
# PostgreSQL connection string for local Docker setup
DATABASE_URL=postgresql://postgres:postgres123@localhost:5435/issue_tracker_db?schema=public

# Alternative für Production (auskommentiert)
# DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# Server Configuration
PORT=3000

# Docker Database Credentials (für Referenz)
POSTGRES_DB=issue_tracker_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres123